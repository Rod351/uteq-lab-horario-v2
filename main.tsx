import React from 'react'
import ReactDOM from 'react-dom/client'
import UTQScheduler from './App'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <UTQScheduler />
  </React.StrictMode>,
)
